# ============================================
# Android Release Signing Configuration
# Copy to keystore.properties and fill in values
# DO NOT commit the real keystore.properties file
# ============================================
storeFile=release-keystore.jks
storePassword=
keyAlias=wuxia-release
keyPassword=
